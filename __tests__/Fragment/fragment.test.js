import React from "react";
import { shallow, render } from "enzyme";
// import {A} from "...";

describe (`Component <A /> & component <B /> interaction`, () => {

	test(`A test that shows that <A /> &
		<B /> play well together`, () => {

		expect(0).not.toEqual(1);
	
	});

});
